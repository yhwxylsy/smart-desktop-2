# 智能桌面 AI 终端｜阶段性总结与答辩推进清单

更新时间：2026-06-22（北京时间）
当前阶段：**公网部署、真实 ESP32 数据接入、微信小程序体验版均已完成；进入答辩验收与彩排阶段。**

## 1. 本阶段结论

项目的核心演示链路已经具备：

```text
你的小程序 / 网页控制台
        ↓ HTTPS
Hugging Face 公网后端 + DashScope AI
        ↓ 本机 ESP32 relay
ESP32S3 → STM32 / 传感器 / 执行器 → ACK 回传
```

- 网页端、小程序端共用同一公网后端。
- 页面不再使用伪造的传感器数值、伪造在线状态或伪造就绪百分比；无真实数据时会如实显示离线、等待或无数据。
- 本机 relay 保留，作为答辩时电脑连接 ESP32 与公网后端之间的桥接，不在本阶段移除。
- 答辩采用“**你本人使用手机扫码展示**”的方式；不继续处理正式上架、备案或给导师添加体验成员。

## 2. 已完成与已验证事项

### 2.1 公网服务与云端 AI

| 项目 | 当前状态 |
| --- | --- |
| Hugging Face Space | 已运行 |
| 公网地址 | `https://yh001399-smart-desktop-ai-terminal.hf.space` |
| 网页控制台 | `https://yh001399-smart-desktop-ai-terminal.hf.space/console` |
| 健康检查 | `https://yh001399-smart-desktop-ai-terminal.hf.space/api/health` |
| AI 服务商 / 模型 | `dashscope_openai` / `qwen-plus` |
| `cloud_ready` | **true** |

2026-06-22 已重新访问公网健康检查，返回：

```json
{
  "status": "ok",
  "protocol": "smart-desktop-realtime-v1",
  "ai_provider": "dashscope_openai",
  "ai_model": "qwen-plus",
  "cloud_ready": true,
  "device_id": "desktop-agent-001",
  "edge_id": "esp32s3-sense-001"
}
```

这说明 Hugging Face Secret 中的 DashScope 配置已经被云端服务正确读取。API Key 和控制令牌均不写入本文件、源码或截图。

### 2.2 真实硬件链路

2026-06-22 通过公网诊断接口读取到的实时快照如下（数值是当时的一次采样，不是页面默认值）：

| 项目 | 快照结果 |
| --- | --- |
| `online` | `true` |
| `uart_ok` | `true` |
| 最近心跳 | `2026-06-21T19:34:11.140976Z`（UTC） |
| 温度 | `27.9 °C` |
| 湿度 | `54 %` |
| 距离 | `10.5 cm` |
| 待执行命令 | `0` |

数据来源为 `GET /api/realtime/diagnostics/desktop-agent-001`。网页端与小程序端应以接口返回值为准；答辩当天数值变化是正常现象。

### 2.3 网页端

- 公网控制台可以查看设备状态、传感器、控制闭环与 AI 对话。
- AI、控制、RFID、ASR 等写操作仍由控制口令保护。
- 读取状态不消耗设备命令；没有 ACK、没有设备心跳或没有传感器上报时不会显示为“已完成”。

### 2.4 微信小程序

- 真实 AppID：`wx7397c01ea9d04c21`。
- 体验版 `1.0.0` 已上传并设为可体验版本。
- 公网合法请求域名与 Socket 域名均已配置为 Hugging Face 地址。
- 小程序默认 API 已指向同一公网后端。
- 小程序头像、简介、服务类目已完成；版本审核已提交，但**本次答辩不依赖正式上架**。

体验二维码文件：

```text
output/playwright/wechat-experience-qr-only-1.0.0.png
```

答辩时由管理员本人扫描即可使用。未加入“体验成员”的其他微信账号不能直接使用体验版，这是微信平台的账号权限规则，不是项目故障。

### 2.5 部署与版本记录

Hugging Face 当前部署已包含以下关键变更：

```text
34eeae5 Merge Space main deployment branch
766368c Fix Space metadata emoji
c7c7b95 Speed up demo polling and add IoTDA uplink
```

当前工作区的 `STAGE_SUMMARY.md` 是本次更新的阶段文档，尚未纳入 Git 提交；其余工作区状态应在提交前单独复核。

### 2.6 P0 最终验收记录（只读核验，2026-06-22 13:13—13:16，北京时间）

本次核验严格**未修改**电脑的网络、VPN、DNS、路由、防火墙或 ESP32 配置；未停止/重启 relay，未下发硬件控制指令。

| 核验项 | 结果 | 证据 / 说明 |
| --- | --- | --- |
| Hugging Face 云端 AI | 通过 | `GET /api/health` 返回 `status=ok`、`ai_provider=dashscope_openai`、`ai_model=qwen-plus`、`cloud_ready=true`。 |
| 设备状态接口 | 字段通过，时效性待复验 | `GET /api/realtime/diagnostics/desktop-agent-001` 与 `/api/state/desktop-agent-001` 均返回 `online=true`、`uart_ok=true`、`edge_id=esp32s3-sense-001`。接口快照包含温度 `27.9 °C`、湿度 `54 %`、距离 `10.5 cm` 等真实上报字段。 |
| 上报新鲜度 | **未通过当场验收** | 两个接口的 `last_seen` 均为 `2026-06-21T19:34:11.140976Z`（北京时间 2026-06-22 03:34:11）；在本次 13:16 核验时约为 **582 分钟前**。当前后端会保留最近一次真实遥测并维持其 `online=true`，故该布尔值不能单独证明此刻 ESP32 正在上报。 |
| 本机 ESP32 relay | 保留且单监听 | 本机 `0.0.0.0:8091` 仅有一个监听 PID（34264）；另一个同启动时刻的 PID（28168）是其父 Python 启动链，未监听该端口，未发现第二个独立 relay 监听器。 |
| 网页控制台（部署版本） | 数据来源审计通过；未完成可视化运行验收 | `GET /console` 返回 HTTP 200。部署页面的 `refresh()` 同时读取 `/api/health`、`/api/state/{deviceId}`、`/api/realtime/diagnostics/{deviceId}`；温湿度/距离仅由返回的 `sensors.temperature_c`、`humidity_pct`、`distance_cm` 渲染，缺失时显示 `-` / `等待上报`。未发现写死的传感器数值、`Math.random`、`mock` 或 `fake` 数据路径。因本机没有供自动化调用的 Chrome，本次未做浏览器可视化截图；这不影响上述部署源码与接口的只读核验。 |
| 微信小程序 | 数据来源审计通过；待手机实测 | 默认 API 为同一 Hugging Face 地址，`refreshState()` 并行请求 `/api/state/{deviceId}`、`/api/realtime/status`、`/api/realtime/diagnostics/{deviceId}` 与 `/api/health`。传感器字段直接取接口 `state.sensors`，初始值均为 `-` / `等待真实接口`，没有传感器数值兜底；代码中的 `Math.random` 仅用于聊天消息 ID。未在本次只读操作中启动微信开发者工具或向手机下发控制指令。 |

**本次结论：**云端配置、接口字段、relay 单监听，以及网页/小程序“无假数据”的实现均已核验；但由于最近真实上报已过约 9 小时 42 分钟，且尚未完成手机与网页的当场可视化复核，本次只能记为“**P0 预验收：条件通过，最终通过待复验**”，不得表述为设备已在核验时刻实时在线。

答辩前需在不改动系统网络配置的前提下，等待 ESP32 自动产生一条新的 heartbeat 或 telemetry，使 `last_seen` 刷新至当天当前时间；随后用网页与体验版小程序分别刷新并确认同一组新数据。若要取得“控制闭环通过”结论，还需由本人执行一条已验证的低风险动作并观察真实 ACK 和设备侧效果。

### 2.7 P0 新鲜度复验（ESP32 重新插入后，2026-06-22 13:20—13:22，北京时间）

ESP32 重新插入后，仅通过公网诊断接口轮询观察链路；未改动网络/VPN/DNS/路由/防火墙，未修改 ESP32 配置，未执行硬件控制指令。

| 核验项 | 结果 | 证据 |
| --- | --- | --- |
| 实时 heartbeat / telemetry | **通过** | 两轮共 9 次读取均观察到 `last_seen` 连续推进：从 `2026-06-22T05:20:20.883334Z` 更新至 `2026-06-22T05:21:58.801489Z`。这与 ESP32 固件约 5 秒 heartbeat、STM32 周期遥测的设计相符。 |
| 设备与 UART 状态 | **通过** | 全部采样维持 `online=true`、`uart_ok=true`。 |
| 真实传感器变化 | **通过** | 复验期间接口数值发生变化：温度 `24.7 → 24.9 °C`、湿度 `63.5 → 60.1 %`、距离 `72.8 → 51.0 cm`；不是静态演示值。 |
| 网页/小程序读取的一致性 | **接口通过** | 13:22 同时读取 `/api/state/desktop-agent-001` 与 `/api/realtime/diagnostics/desktop-agent-001`，两者 `last_seen` 完全一致，均为 `2026-06-22T05:21:58.801489Z`；`cloud_ready=true`。网页和小程序均以这两个状态接口为数据源。 |

**复验结论：**此前的陈旧快照问题已在 ESP32 重新插入后消失；当前 ESP32 → 本机 relay → Hugging Face → 网页/小程序的数据链路恢复实时更新，P0 的“云端就绪、设备在线、UART 正常、真实传感器数据”条件通过。仍保留两项现场步骤：在网页和本人微信体验版中各刷新一次确认画面；如需宣称“控制闭环已当日验收”，再由本人执行已验证的低风险动作并观察 ACK 与实体设备效果。

### 2.8 答辩成品化自检（2026-06-22 13:26，北京时间）

为避免现场把历史快照误讲为实时数据，新增只读脚本 `tools/realtime_readiness_check.py`，并已写入 `docs/DEMO_STORYBOARD.md` 与 `README.md` 的开场步骤。脚本仅读取云端健康、设备状态和诊断接口，要求：

- `cloud_ready=true`、`online=true`、`uart_ok=true`；
- state 与 diagnostics 的最近上报均不超过 20 秒；
- 至少两项真实传感器字段存在，且两接口的 `edge_id` 一致。

实测默认阈值返回 `verdict=PASS`：状态上报年龄 `7.8 秒`、诊断上报年龄 `2.2 秒`，温度 `25.2 °C`、湿度 `58.5 %`、距离 `72.4 cm` 等字段存在。将阈值人为设为 0 秒时，脚本按预期返回 `verdict=FAIL` 与 `state_is_fresh` / `diagnostics_is_fresh`，证明它不会把陈旧数据误判为可演示。

### 2.9 实时链路波动记录（2026-06-22 14:13—14:14，北京时间）

一次后续只读自检返回 `verdict=FAIL`：`last_seen=2026-06-22T06:12:36.863364Z`，在 14:13 检查时已约 `79.8 秒` 未更新；随后间隔约 5 秒的 4 次诊断读取均未变化。云端仍返回 `cloud_ready=true`、`online=true`、`uart_ok=true`，但这些是最后一次真实快照，**不能作为当场实时演示依据**。

本机 relay 仍唯一监听 `0.0.0.0:8091`，因此未发现 Hugging Face 或 relay 进程退出。Windows 当前只枚举到蓝牙串口（COM3—COM6），未看到可识别的 ESP32 USB 串口；这不单独证明 ESP32 故障，但结合上报停止，需优先检查 ESP32 的供电、Wi-Fi 与其到 relay 的上报路径。恢复后必须重新运行 `python .\\tools\\realtime_readiness_check.py`，只有返回 `PASS` 才恢复答辩演示。

### 2.10 ESP32 重新插入复验（2026-06-22 18:39—18:42，北京时间）

ESP32 重新插入后，heartbeat/telemetry 于 18:39 恢复，温湿度与距离再次变化。启动早期 `uart_ok` 曾暂为 `false`；随后在 18:42 的连续观察窗口恢复为 `true`。最终执行 `python .\\tools\\realtime_readiness_check.py` 返回 `verdict=PASS`：

- `cloud_ready=true`、`online=true`、`uart_ok=true`；
- state 与 diagnostics 的 `last_seen` 一致，检查时均仅 `4.3 秒` 前；
- 实时快照为温度 `26.4 °C`、湿度 `53.7 %`、距离 `9.0 cm`，并含 7 项传感器字段。

结论：重新插入后的云端实时链路与 UART 保活均恢复。答辩现场若刚重新供电，应先预留 1—3 分钟给 ESP32 建立 Wi-Fi/UART 保活；“30 秒自检”仅指脚本执行和判读时间，不包括硬件启动等待。

### 2.11 一键答辩启动器（2026-06-22）

新增 `tools/start_defense_demo.cmd` / `tools/start_defense_demo.ps1`，把答辩前原本分散启动的流程按安全顺序串联：

1. 保留已监听的本机 relay；只有 relay 缺失时才后台启动一份。
2. 轮询 `realtime_readiness_check.py`，直到云端、ESP32、UART、上报新鲜度和传感器字段全部通过。
3. 打开 Hugging Face 网页控制台。
4. 经人工确认后，在独立窗口启动笔记本麦克风的持续语音监听器；可用 `-VoiceMode wakeword` 改为短窗口“唤醒词 → 指令录音”模式。

该启动器不修改网络、VPN、DNS、路由、防火墙或 ESP32 配置，不自动发送聊天、硬件动作或 ACK，也不会重复启动已存在的 relay/语音前端。已用 `-SkipVoice -NoBrowser` 完成全流程非侵入验证：短暂 `uart_ok=false` 与一次 HTTPS EOF 均被重试逻辑正确拦截，最终只读自检返回 `PASS`。默认语音输入设备 `1` 已列出为本机数字麦克风；实际启动语音前端时仍需由本人确认录音提示并完成一次现场口语测试。

## 3. 已明确不在本阶段推进的事项

这些不是“没完成”，而是为了答辩可控性主动收束范围：

1. **不正式发布小程序。** 不做备案、上架、正式用户开放和全网搜索。
2. **不为导师单独开体验成员。** 由你本人使用体验版在现场演示即可。
3. **不迁移到付费服务器或国内云 WSS。** 当前免费 Hugging Face 公网后端 + 本机 relay 可支撑答辩。
4. **不在答辩前改网络、VPN、DNS、路由或防火墙。** 现有 VPN 是电脑联网与协作的重要条件，只做项目内应用配置与只读检查。
5. **不为了“看起来稳定”补假数据。** 真实链路暂时离线时，应展示实际离线状态并排查设备/relay。

## 4. 接下来的推进方向

本项目不再做大范围架构调整，优先级按下面顺序执行。

### P0：答辩前最终验收（必须完成）

1. 保持 ESP32 插在答辩电脑上，确认电脑网络与 VPN 正常；不要改系统网络设置。
2. 保持本机 relay 进程运行；如果电脑重启，再按既有配置启动 relay，且不要同时开启两个 relay 实例。
3. 打开健康检查，确认 `cloud_ready=true`。
4. 打开公网诊断接口，确认 `online=true`、`uart_ok=true`，并看到本次真实传感器值。
5. 用自己的微信扫描体验二维码，确认小程序能加载真实状态。
6. 用网页控制台执行一次低风险、可观察的真实动作，确认设备侧效果和 ACK 回传；只演示已经验证过的动作。
7. 用小程序重复查看设备状态或执行同一条已验证动作，证明两端读取的是同一台设备、同一条云端链路。

### P1：准备答辩叙事（建议完成）

推荐 3—5 分钟演示顺序：

1. 展示实体硬件与 ESP32 连接状态。
2. 用浏览器打开 `/api/health`，说明云端 AI 为 DashScope、`cloud_ready=true`。
3. 打开网页控制台，展示实时温湿度/距离和设备在线状态。
4. 扫体验二维码进入小程序，展示同一组真实状态。
5. 触发一项已验证的设备动作，展示“指令 → 设备执行 → ACK/状态刷新”的闭环。
6. 如网络或某个传感器临时波动，直接说明其真实状态与数据来源，不用补演示数值。

### P2：准备故障兜底（建议完成）

- 手机保存体验二维码图片，避免现场临时找后台。
- 浏览器预先收藏健康检查、诊断接口和网页控制台。
- 准备一根可用 USB 数据线和 ESP32 供电备份。
- 提前到答辩地点测试网络；若现场网络限制访问 Hugging Face，使用你已验证可联网的手机热点或 VPN 方案，但不要在临场修改电脑系统网络配置。
- Hugging Face 免费 Space 可能冷启动。演示前 5—10 分钟先打开一次网页控制台，让服务提前唤醒。

## 5. 答辩当天最小检查表

```text
[ ] ESP32 已插入，硬件已供电
[ ] 本机 relay 正在运行
[ ] VPN / 网络保持原有可用状态（不改系统网络配置）
[ ] /api/health 显示 cloud_ready=true
[ ] diagnostics 显示 online=true、uart_ok=true
[ ] 网页控制台刷新后显示真实传感器数据
[ ] 手机已能用体验二维码进入小程序
[ ] 至少一条硬件动作已在当天重新验收
[ ] 二维码、网页控制台、健康检查均已收藏或打开
```

## 6. 后续可选工作（答辩后再做）

- 迁移至国内云服务器并提供稳定 HTTPS/WSS，以降低公网链路和免费休眠带来的等待。
- 正式发布小程序、完成备案/审核后向任意用户开放。
- 如需给导师或同学独立试用，在微信公众平台添加其微信号为“体验成员”。
- 为遥测、RFID 和日志接入持久化存储，避免免费 Space 重启造成内存数据丢失。
- 在不影响真实数据原则的前提下，补充可追溯的历史趋势图和系统自检页。

## 7. 核心原则

答辩时宁可显示真实的“离线 / 无数据 / 等待 ACK”，也不展示编造的正常数据。项目的价值在于：网页端、小程序端、云端 AI 与实体设备使用同一条可验证链路。
